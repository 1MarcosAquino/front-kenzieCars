import styled from 'styled-components';

const StyledHeader = styled.main`
    background-color: #00ff117f;
`;

export const Body = () => {
    return (
        <StyledHeader>
            <h1>MyBody</h1>
        </StyledHeader>
    );
};
